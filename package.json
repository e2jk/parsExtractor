{
  "name": "parsExtractor",
  "version": "0.1.1",
  "description": "Parse HL7® messages and easily extract fields to a CSV file.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/icons/icon.icns --out=release-builds",
    "package-win": "electron-packager . parsExtractor --overwrite --platform=win32 --arch=ia32 --icon=assets/icons/icon.ico --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"parsExtractor\"",
    "package-linux": "electron-packager . parsExtractor --overwrite --platform=linux --arch=x64 --icon=assets/icons/icon.png --out=release-builds"
  },
  "keywords": [
    "HL7",
    "parse",
    "extract"
  ],
  "author": {
    "name": "Emilien Klein",
    "email": "emilien@klein.st"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/e2jk/parsExtractor.git"
  },
  "devDependencies": {
    "electron": "2.0.2",
    "electron-packager": "^12.1.0"
  },
  "dependencies": {
    "hl7-dictionary": "^1.0.1"
  }
}
